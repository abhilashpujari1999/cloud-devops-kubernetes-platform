apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: devops-demo-app-ingress
spec:
  rules:
    - http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: devops-demo-app-service
                port:
                  number: 80